import 'package:flutter/material.dart';

class HomePageStudent extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Home Screen Student'),
      ),
      body: Center(
        child: Text('Home Screen Content'),
      ),
    );
  }
}
